<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android">

    <RelativeLayout
        android:id="@+id/comment_loading_layout"
        android:layout_width="match_parent"
        android:layout_height="96dp"
        android:visibility="gone">

        <RelativeLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_centerInParent="true">

            <ProgressBar
                android:id="@+id/comment_loading_progressBar"
                style="@style/commentProgressBarStyle"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:layout_marginRight="@dimen/outerInterval"
                android:clickable="false"
                android:indeterminate="true"></ProgressBar>

            <TextView
                android:id="@+id/comment_loading_hint"
                android:layout_width="wrap_content"
                android:layout_height="match_parent"
                android:layout_toRightOf="@+id/comment_loading_progressBar"
                android:gravity="center|right"
                android:text="@string/loading"
                android:textColor="@color/light_grey"
                android:textSize="@dimen/font13sp" />
        </RelativeLayout>
    </RelativeLayout>

</merge>